#[allow(unused_imports)]
use recs::{EntityId, component_filter};
use crate::*; 

#[allow(dead_code, unused_variables)]
pub fn run(window: &mut three::Window, store: &mut recs::Ecs) {
    //TODO: switch screens
}